# Curated variable → classification links (#416 tail) — see
# reg_meta_build/classification_links.py.
#
# The build-time code-set-containment detector
# (`classifications.link_value_set_classifications`) auto-links the CONFIDENT
# tier: value sets whose inline codes unambiguously enumerate ONE classification
# (single-family AND (≥15 distinct codes OR ≥0.90 (code,label) agreement)). The
# residue is the family-ambiguous short numeric sets (SNI / SSYK / SUN coincide
# below ~15 codes) the detector deliberately leaves for a human. THIS file is the
# curated override for that tail.
#
# Each [[link]] entry names a single variable and the classification its inline
# codes belong to:
#
#   variable       = "provider/register/variable"   # 3-segment FQID
#   classification = "<short_name>"                  # a seeded classification
#   note           = "..."                           # optional, provenance
#
# Precedence: a curated link OVERRIDES every auto/feed candidate for the
# variable's value-set state keys (delete-then-insert into
# `classification_candidate`, before the auto detector runs). References resolve
# against the built DB at materialize time; an unresolved FQID or short_name
# FAILS the build (EXIT_CONFIG) — fix the entry, don't ignore it. An entry whose
# provider isn't in the build is SKIPPED (a partial --providers build can't
# represent it). An empty file is a no-op — no links written.
#
# #494 part 2 — the FIRST curated residue entries. The vintage-period reclaim
# (part 1) clears the same-family-across-vintages residue; what's left is genuine
# cross-family numeric coincidence. The entries below are the SAFE subset of that
# tail: residual value sets whose LABELS uniquely identify ONE STANDALONE
# (single-edition, no per-year vintage) seeded classification — so there is exactly
# one correct short_name and no "which vintage" choice. Verified against a real
# scb,sos build (label agreement ≥0.90 to the named class, <0.90 to every other
# candidate). The bulk of the residue (county/LKF codes — a per-year vintage with
# no single short_name; short numeric sets whose true family isn't even a
# candidate) is NOT single-short_name-curatable and stays for the deferred
# follow-up.

# Institutional sector (SEKTOR2000) — codes 12/13/14/21/… ("Statliga affärsverk",
# "Kommunal förvaltning", "Landsting"/"Region", "Aktiebolag …") coincide numerically
# with SNI/SSYK/LKF division codes, but the labels are unambiguously sector.
[[link]]
variable       = "scb/arbetskraftsbarometern/sektorkod"
classification = "SEKTOR2000"
note           = "curated:#494 label-unambiguous sector codes (coincide cross-family by digits)"

[[link]]
variable       = "scb/fortroendevalda/sektor"
classification = "SEKTOR2000"
note           = "curated:#494 label-unambiguous sector codes"

[[link]]
variable       = "scb/kommunalekonomisk-utjamning/sektor"
classification = "SEKTOR2000"
note           = "curated:#494 label-unambiguous sector codes"

[[link]]
variable       = "scb/lisa/ast-sektorkod"
classification = "SEKTOR2000"
note           = "curated:#494 label-unambiguous sector codes"

[[link]]
variable       = "scb/lisa/org-sektorkod"
classification = "SEKTOR2000"
note           = "curated:#494 label-unambiguous sector codes"

[[link]]
variable       = "scb/lisa/sektorkod"
classification = "SEKTOR2000"
note           = "curated:#494 label-unambiguous sector codes"

[[link]]
variable       = "scb/rams/institutionell-sektorkod"
classification = "SEKTOR2000"
note           = "curated:#494 label-unambiguous sector codes"

[[link]]
variable       = "scb/yrkesreg/sektor-ku1"
classification = "SEKTOR2000"
note           = "curated:#494 label-unambiguous sector codes"

[[link]]
variable       = "scb/yrkesreg/sektorkod"
classification = "SEKTOR2000"
note           = "curated:#494 label-unambiguous sector codes"

[[link]]
variable       = "scb/yrkesreg/sektorkod-2"
classification = "SEKTOR2000"
note           = "curated:#494 label-unambiguous sector codes"

[[link]]
variable       = "scb/yrkesreg/sektorkod-storsta-forvarvskalla"
classification = "SEKTOR2000"
note           = "curated:#494 label-unambiguous sector codes"

# Education (ISCED) — the variable slug itself names the classification; the codes
# coincide with other numeric families but the labels are the ISCED level / field
# names verbatim.
[[link]]
variable       = "scb/ureg/isced2011niva"
classification = "ISCED2011"
note           = "curated:#494 ISCED 2011 education-level labels"

[[link]]
variable       = "scb/ureg/isced-f-2013"
classification = "ISCED-F2013"
note           = "curated:#494 ISCED-F 2013 field-of-education labels"
