"""reg_webapp: FastAPI backend serving the reg_meta catalog to the SPA.

See ``../DESIGN.md`` for the boot seam, steward layering, and the
Pydantic boundary. A5.1a ships the boot path + ``GET /api/context`` only.
"""

__version__ = "0.1.0"
