"""FastAPI routers for reg_webapp.

A5.1a ships only ``context``. Catalog browse routers land in A5.1b — and the
``{fqid:path}`` catch-all does NOT exist yet (A5.1a invariant: no unguarded
catch-all route).
"""
