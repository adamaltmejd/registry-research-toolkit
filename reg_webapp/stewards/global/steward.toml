# The `global` steward is special (§9.1): it ships NO steward.project_data.json,
# so the catalog is reg_meta's full universe (no filter). Its absence is what
# the loader keys on to select full-universe mode.
id = "global"
name = "Global"
long_name = "Swedish Register Research Catalog (full universe)"
hostname = "global.example.org"
