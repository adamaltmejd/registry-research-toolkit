interface:
  display_name: "Web Design Reviewer"
  short_description: "Review rendered web UI quality gates"
  default_prompt: "Use $web-design-reviewer to review rendered UI routes before PR handoff."
policy:
  allow_implicit_invocation: true
