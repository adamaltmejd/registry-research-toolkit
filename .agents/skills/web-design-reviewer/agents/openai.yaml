interface:
  display_name: "Web Design Reviewer"
  short_description: "Review reg_webapp screenshots for PR gates"
  default_prompt: "Use $web-design-reviewer to render and review reg_webapp UI routes before PR handoff."
policy:
  allow_implicit_invocation: true
