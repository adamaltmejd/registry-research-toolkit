interface:
  display_name: "Upgrade Registry Dependencies"
  short_description: "Upgrade toolkit dependencies outside Yard"
  default_prompt: "Use $upgrade-deps to update registry toolkit dependencies and verify the changes without a Yard lane."
