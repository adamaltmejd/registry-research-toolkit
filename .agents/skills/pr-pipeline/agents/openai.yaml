interface:
  display_name: "Registry PR Pipeline"
  short_description: "Build registry issues into PRs"
  default_prompt: "Use $pr-pipeline issue 510 to take a registry issue through implementation and PR readiness."
policy:
  allow_implicit_invocation: true
