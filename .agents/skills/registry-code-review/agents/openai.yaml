interface:
  display_name: "Registry Code Review"
  short_description: "Callable registry PR review workflow"
  default_prompt: "Use $registry-code-review to review a Registry PR or diff."
policy:
  allow_implicit_invocation: false
